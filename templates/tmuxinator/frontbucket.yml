# /Users/akofink/.config/tmuxinator/frontbucket.yml

name: frontbucket
root: ~/dev/repos/atlassian/frontbucket

windows:
  - zsh: nvm use
  - vim:
    - nvm use
    - vim
  - yarn:
    - nvm use
    - yarn && yarn start --https
