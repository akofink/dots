name: pane-names (a test! | let's find out)
root: ~/dev/sandbox

windows:
  - editor (and others):
      layout: tiled
      panes:
        - vim and things:
          - vim
        - mostly just docker (maybe compose):
          - docker ps
