name: dss-secrets-store
root: ~/dev/repos/atlassian/dss-secrets-store

pre_window:
  - sdk env

windows:
  - zsh:
      layout: even-vertical
      panes:
        - # mvn clean verify -DskipTests && atlas nebulae run -s resources-only
        - # (source envs.sh && mvn spring-boot:run)
        - zsh:
  - vim: vim
  - zsh:
