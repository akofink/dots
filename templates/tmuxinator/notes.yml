# /Users/akofink/.config/tmuxinator/notes.yml

name: notes
root: ~/notes

windows:
  - zsh:
      layout: main-horizontal
      panes:
        - zsh:
        # - ollama: ollama serve
        # - codex:
        #     - nvm use --lts
        #     - codex --provider ollama --model llama3.2
  - vim:
    - vim
    - ' n'
  - http:
      root: ~/dev/http
      layout: main-horizontal
      panes:
        - http:
        - vim:
            - nvm use --lts
            - vim
            - ' n'
  - mock-ssh-keys:
      root: ~/dev/mock-ssh-keys
      layout: main-horizontal
      panes:
        - zsh:
        - vim:
            - vim
            - ' n'
  - devbucket-git:
      root: ~/tmp/devbucket-repos/
      layout: main-horizontal
      panes:
        - zsh:
  - atlas:
  - docker:
  - psql:
  - ads: which ads_setup && ads_setup

