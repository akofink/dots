name: dss-event-delivery
root: ~/dev/repos/atlassian/dss-event-delivery

pre_window:
  - sdk env

windows:
  - zsh:
      layout: even-vertical
      panes:
        - atlas nebulae sandbox run -s resources-only
        - (source envs.sh && mvn spring-boot:run)
        - zsh:
  - vim: vim
  - zsh:
